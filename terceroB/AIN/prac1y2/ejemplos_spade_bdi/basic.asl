!start.
+!start <-
    +car(rojo);
    +truck(azul).
+car(Color)
    <- .print("El carro es ",Color).!start.
+!start <-
    +car(rojo);
    +truck(azul).
+car(Color) 
    <- .print("El carro es ",Color).
